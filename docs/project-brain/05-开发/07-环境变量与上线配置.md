# 环境变量与上线配置

- 状态：v0.84 已建立
- 最后更新：2026-06-30
- 负责人：开发专项
- 关联页面：全站 WhatsApp、Footer、隐私页、联系表单、图册弹窗表单
- 下一步：用户确认真实 WhatsApp、业务邮箱和表单接收端点后，写入本机 `.env.local` 或部署平台环境变量。

## 当前原则

真实联系方式和表单接收地址不写死在源码里。当前源码只保留本地演示 fallback，正式上线前必须通过环境变量替换。

## 环境变量

| 变量 | 用途 | 是否公开 | 当前状态 |
|---|---|---|---|
| `PUBLIC_HYMUEBLE_WHATSAPP_URL` | 全站 WhatsApp 浮标、Footer、联系页 CTA | 是，生成进静态 HTML | 待真实号码 |
| `PUBLIC_HYMUEBLE_EMAIL` | Footer、隐私页、Organization JSON-LD | 是，生成进静态 HTML | 待确认业务邮箱 |
| `PUBLIC_HYMUEBLE_FORM_ACTION` | 联系页和图册弹窗表单提交地址 | 是，生成进静态 HTML | 待选择表单服务/CRM/后端 |

示例文件：

- `.env.example`

本机真实值应放入：

- `.env.local`

`.env.local` 已在 `.gitignore` 中排除，不应提交。

## 本地演示模式

不配置环境变量时：

- WhatsApp 使用占位号码。
- 邮箱使用 `proyectos@hymueble.com` 作为演示默认值。
- 表单不提交到外部服务。
- 表单保留 `data-demo-form`，前端脚本拦截提交并显示西语成功反馈。

这适合本地老板演示，不代表正式上线可投放。

## 上线配置模式

配置三项环境变量并重新构建后：

- WhatsApp 链接替换为真实 URL。
- 邮箱替换为确认后的业务邮箱。
- 表单生成 `action="..."`。
- 表单不再带 `data-demo-form`，不会被本地演示脚本拦截。

上线前必须运行：

```bash
pnpm build
pnpm qa:static
pnpm qa:catalogs
pnpm qa:launch
```

`pnpm qa:launch` 会检查：

- 环境变量是否存在。
- WhatsApp 是否仍为占位号码。
- 生成 HTML 是否仍有演示表单标记。
- 表单 action 是否仍为示例地址。

## v0.84 验证

使用非真实测试值完成过一次上线配置构建：

- `PUBLIC_HYMUEBLE_WHATSAPP_URL=https://wa.me/12345678901?...`
- `PUBLIC_HYMUEBLE_EMAIL=ventas@hymueble.test`
- `PUBLIC_HYMUEBLE_FORM_ACTION=https://forms.hymueble.test/project-inquiry`

结果：

- `pnpm build`：通过。
- `pnpm qa:launch`：通过。
- 生成 HTML 中 demo forms：0。
- 占位 WhatsApp：0。

随后已重新执行普通本地演示构建，确保当前本地预览仍保持演示模式。
