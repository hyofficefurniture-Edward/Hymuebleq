# 页面开发状态

- 状态：进行中
- 最后更新：2026-06-30
- 负责人：开发专项
- 关联资料：当前 Astro v0.94
- 关联页面：全站
- 下一步：继续人工复核 v0.94 演示效果，并补齐真实 WhatsApp、表单接收端和正式部署配置

## 全站能力补充

- v0.18 已在 `BaseLayout` 统一补齐 canonical、robots、Open Graph、Twitter Card、主题色和 Organization JSON-LD。
- 首页、酒店页、酒店图册页、酒店案例页、工厂页、展厅页、联系页、资源页、动态行业页、目录页和案例详情页均已绑定页面级分享图。
- 2026-06-29 静态审计覆盖 34 个 HTML：metadata、OG 图片、本地图片、内部链接和同页重复图均通过。
- v0.19 已新增 `robots.txt` 与 `sitemap.xml` 动态 endpoint；当前 sitemap 覆盖全部 35 个生成页面，且本地 dev server 可访问。
- v0.20 已完成 6 个核心页桌面/移动端快速 QA；办公、医疗、教育行业入口页新增对应 Matterport 展厅模块。
- v0.22 已完成全站 Hymueble 图片 WebP 化；构建后 34 个 HTML 页面无非 WebP 图片引用，无缺失图片，无断链。
- v0.23 已完成图片加载策略：34 个页面所有 `<img>` 均具备 `loading` 和 `decoding="async"`，首屏主视觉具备 `fetchpriority="high"`。
- v0.24 已完成主视觉 preload：每个页面一个本地相对 WebP preload；本地预览不再预加载生产域名图片。
- v0.25 已增强项目解决方案表达。
- v0.26 已根据第一轮验收反馈拆清首页与项目页边界：首页保留轻量采购路径分流，完整项目解决方案模块保留在 `/proyectos/`。
- v0.27 已增强首页质量控制模块，补齐 6 个采购风险检查点。
- v0.28 已接入鸿业盛大公司 logo WebP 到全站 Header 和 Organization JSON-LD。
- v0.59 已按 Georgebuild 参考方向完成全站排版系统优化：扩大容器和 section 呼吸感、统一大标题阶梯、圆角胶囊 CTA、浅色编辑式 Hero、图像主导卡片、深色 showroom 体验区、资源视频页双栏修正和 footer 节奏优化。构建通过，桌面关键页无横向溢出和缺图；移动端流程模块保留 2 行横向滑动交互。
- v0.77 已将全站 Matterport 展厅入口改为站内弹窗 iframe，不再跳转新标签页；`pnpm qa:static` 覆盖 35 个 HTML、35 个 sitemap 路由和 21 个展厅入口。
- v0.78 已修复 1280px 中等桌面 Header / 合作品牌 rail 页级溢出，以及 390px 手机端 section-head / 横向 rail 页级溢出；内部横向内容保留自身滚动。
- v0.79 已收敛 `/proyectos/` 项目路线卡片正文和 `/contacto/` 辅助说明密度，减少 4-6 行长段落，并把联系页行内样式收束为可复用 CSS。
- v0.80 已完成 14 条代表路由桌面/手机长段落阈值审计，压缩重复 CTA 与辅助文案，并清理页面/组件中的旧行内文本样式。
- v0.81 已确认 Matterport 展厅入口使用站内弹窗留住客户，并在 `pnpm qa:static` 中加入 iframe fullscreen 配置防回退检查。
- v0.82 已为图册弹窗增加请求来源上下文：客户点击不同图册 CTA 时，弹窗标题和隐藏字段 `catalog_interest` 会记录具体图册或分类；`pnpm qa:static` 覆盖 71 个图册触发器。
- v0.83 已建立图册 PDF manifest 与 `pnpm qa:catalogs`：21 份 PDF 源文件覆盖五个行业，41 个渲染后的图册兴趣值均有内部源文件或人工跟进映射。
- v0.84 已建立上线联系方式配置：WhatsApp、邮箱和表单 action 可通过公开环境变量注入；本地演示模式继续显示成功反馈，上线配置模式通过 `pnpm qa:launch` 验证 0 demo forms / 0 placeholder WhatsApp。
- v0.85 已新增 `pnpm qa:browser` 真实浏览器 QA gate：覆盖 11 条核心路由桌面/移动端，检查横向溢出、坏图、半成品残留、超长可见段落、移动菜单、Showroom 弹窗、图册弹窗和联系表单反馈。
- v0.86 已修复移动/平板端 Header 与菜单定位：Header 改为 fixed，页面补顶部占位，菜单从 Header 下方展开；`/fabrica/` 页脚附近打开汉堡菜单的场景已纳入 `pnpm qa:browser`。
- v0.87 已压缩首页移动端信任数据区：手机端展示为 2 行 2 列，四个核心指标完整可读；第五个人员规模指标仅桌面端保留。390px 浏览器截图和完整 QA 回归均通过，并已加入 `pnpm qa:browser` 防回退。
- v0.88 已给共享 `ProcessSteps` 流程组件加入 12 个语义线性图标，覆盖报价、测量、设计、材料、样品、生产、质检、终检、包装、物流、安装、售后；桌面和移动端流程布局保持稳定，并加入 `pnpm qa:browser` 防回退。
- v0.89 已新增共享 `FAQTabs` 组件，首页 FAQ 改为 4 类 Tab + Accordion；FAQ 文案集中在 `faqCategories` 数据对象中维护，移动端分类按钮横向滚动，并加入 `pnpm qa:browser` 防回退。
- v0.90 已按新增工厂设备资料重做首页 Fábrica 与 `/fabrica/`：优先使用 HOMAG、Bacci、六面钻、数控加工、无尘/水性喷涂、质检包装等真实设备 WebP；合作品牌 Logo 区改为两行横向滚动，FAQ 标题居中平衡；完整构建、静态 QA、图册 QA 和浏览器 QA 均通过。
- v0.91 已新增共享 `ScrollRail` 组件和全站滚动轨道按钮控制：主要横向滑动模块加入左右前进/后退按钮，仅在内容可滚动时显示，并已把交互检查纳入 `pnpm qa:browser`。
- v0.92 已修复 FAQ 移动端与首页 Showroom 桌面端布局回归：FAQ 分类按钮在手机端改为 2x2 网格且无横向溢出；Showroom 卡片轨道重新跨满内容区，桌面端四卡平均铺开，非溢出状态不显示轨道按钮。
- v0.93 已更新 Footer 社媒入口与移动端排版：删除 Pinterest / X，新增 TikTok，手机端 Footer 品牌、社媒、导航和联系信息居中显示；社媒集合和居中状态已纳入 `pnpm qa:browser`。
- v0.94 已增强上线配置 QA：`qa:launch` 除了检查真实 WhatsApp、邮箱和表单接收端，也会检查 TikTok 社媒入口存在并阻止 Pinterest/X 回流。

| 页面 | 策划状态 | 素材状态 | 开发状态 | 验收状态 | 备注 |
|---|---|---|---|---|---|
| `/` | 已按策划案六次校正 | 真实素材 + 生成视觉 + 多角度案例图 + 公司 logo + 真实设备图 | v0.93 已开发 | 当前通过 | 已补视频封面、采购路径分流、4 个案例、showroom、设备化工厂证据、合作/认证信任条、真实质量流程图、6 个质量检查点、12 步项目交付流程、案例事实字段；移动端信任数据区已改为 2x2；流程卡片已加入语义图标；FAQ 已改为分类 Tab + Accordion；合作品牌、证书图、Showroom、项目卡和流程轨道已加左右按钮；FAQ 手机端与 Showroom 桌面端布局回归已修复；Footer 社媒与移动端居中排版已更新 |
| `/hoteles/` | 已按策划案三次校正 | 真实 WebP + 酒店案例图 | v0.16 已开发 | 当前通过 | 已补酒店目录、VR、案例事实字段、合作背书信任条和 12 步酒店项目服务流程 |
| `/catalogo/` | 已按策划案补齐 | 真实素材 + 生成视觉 | v0.82 已开发 | 当前通过 | 五大品类图册枢纽；图册 CTA 已记录请求来源 |
| `/catalogo/hoteleria/` | 已按策划案二次校正 | 真实 WebP/JPG + 生成视觉 | v0.82 已开发 | 当前通过 | 弹窗、VR、六类酒店目录；目录卡图已与酒店页和分类页分角色，mock-up 段已换真实图；图册弹窗记录 `Catálogo hotelero` |
| `/catalogo/hoteleria/*` | 页面树补齐 | 真实 WebP/JPG | v0.82 已开发 | 当前通过 | 六个酒店分类轻量详情页；bar/conference 支持图已修正；分类图册 CTA 记录具体分类 |
| `/proyectos/` | 已按策划案补齐 | 真实素材 + 生成视觉 + 分场景项目图 | v0.26 已开发 | 当前通过 | 项目解决方案枢纽；已保留完整项目路线卡，行业区改为快速入口；项目卡已补项目类型、空间范围和证据来源 |
| `/proyectos/hoteleria/` | 已按策划案三次校正 | 真实素材 + 生成视觉 + 酒店案例多角度图 | v0.16 已开发 | 当前通过 | 酒店案例卡已补类型、范围、证据字段；首屏已与酒店入口页分图 |
| `/proyectos/*/*/` | 轻量补齐 | 真实 WebP/JPG + 详情支持图 | v0.16 已开发 | 当前通过 | 现有 4 个案例详情可访问，已补摘要字段、证据标签和多角度图；年份/房间数不编造 |
| `/fabrica/` | 已按策划案补齐 | 真实设备 WebP + 真实工序图 + 资质图 | v0.91 已开发 | 当前通过 | 已重做为工厂设备证据页：HOMAG 封边、Bacci 五轴 CNC、六面钻、数控板式加工、无尘/水性喷涂、质检包装；删除重复展厅和重复椅子车间图；工序和资质横向轨道已加左右按钮 |
| `/showroom/` | 已按用户展厅链接补齐 | Matterport 缩略图 + 合成 Hero | v0.81 已开发 | 当前通过 | 四个展厅入口已可点击，已接真实 Matterport 缩略图；Matterport 在站内弹窗打开，不新标签外跳 |
| `/contacto/` | 已按策划案三次校正 | 项目咨询视觉 + 真实出口准备图 | v0.17 已开发 | 当前通过 | 表单反馈已验证；新增空间范围、项目阶段、数量、周期、预算字段；真实 WhatsApp 未接入 |
| `/oficinas/`、`/salud/`、`/educacion/`、`/residencial/` | B 类轻量版 | 已使用真实 WebP + 展厅缩略图 | v0.20 已开发 | 当前通过 | 行业入口不再是纯占位；办公、医疗、教育已加入对应 Matterport 展厅入口，住宅暂无展厅链接 |
| `/catalogo/oficinas/`、`/catalogo/salud/`、`/catalogo/educacion/`、`/catalogo/residencial/` | B 类轻量版 | 已使用真实 WebP | v0.8 已开发 | 当前通过 | 二级目录通路已闭合 |
| `/proyectos/oficinas/`、`/proyectos/salud/`、`/proyectos/educacion/`、`/proyectos/residencial/` | B 类轻量版 | 已使用真实 WebP | v0.8 已开发 | 当前通过 | 二级案例通路已闭合 |
| `/recursos/`、`/recursos/blog/`、`/recursos/videos/` | C 类入口 | 已使用真实 WebP | v0.8 已开发 | 当前通过 | 主资源页已补齐，Blog/Video 仍是轻量页 |
