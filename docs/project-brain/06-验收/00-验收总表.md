# 验收总表

- 状态：进行中
- 最后更新：2026-06-30
- 负责人：验收专项
- 关联资料：本地预览、页面建设说明、测试结果
- 关联页面：全站
- 下一步：继续人工复核 v0.94 演示效果，并确认正式 TikTok 账号、WhatsApp 与表单接收方式

| 轮次 | 日期 | 范围 | 结论 | 备注 |
|---|---|---|---|---|
| 第一轮 | 2026-06-29 | v0.1 核心页面、表单、弹窗、移动端 | 有条件通过 | 可用于本地初步演示，但需资料重审 |
| 第二轮 | 2026-06-29 | v0.28 本地演示版：公司 logo、首页路径边界、质量控制链、Showroom、核心页面、移动端 Header、静态构建 | 有条件通过 | 可用于老板本地演示；真实 WhatsApp、表单后端、正式部署、部分素材替换仍待下一阶段 |
| 第三轮 | 2026-06-29 | v0.32 Header 视觉修正、全站 WhatsApp 浮动入口、Showroom 转化路径、静态构建与截图 | 有条件通过 | Header 更适合老板演示；WhatsApp 仍需真实号码，表单仍为前端演示 |
| 第四轮 | 2026-06-29 | v0.34 Header/CTA 复查、全站静态链接与图片审计、本地预览可访问性 | 有条件通过 | 可继续用于老板演示；真实 WhatsApp 与表单后端仍属上线前任务 |
| 第五轮 | 2026-06-29 | v0.35 首页 12 步流程桌面 6 列 2 行、流程文案精简、桌面/手机响应式复查 | 有条件通过 | 视觉更紧凑，适合老板演示；手机仍保持单列阅读 |
| 第六轮 | 2026-06-29 | v0.36 移动端 12 步流程改为 2 行横向滑动，桌面 6 列 2 行保持不变 | 有条件通过 | 移动端版面高度显著缩短；用户需横向滑动查看完整流程 |
| 第七轮 | 2026-06-29 | v0.37 移动端 Header 与菜单尺寸调整、完整导航可见性复查 | 有条件通过 | 390px 手机展开菜单后可看到完整导航到 `Contacto`，无横向溢出 |
| 第八轮 | 2026-06-29 | v0.38 全站页脚升级：项目 CTA、品牌卡、行业入口、采购路径、能力标签、响应式复查 | 有条件通过 | 页脚更接近参考模板的转化型结构；390px 手机无横向溢出，页脚图片无断图 |
| 第九轮 | 2026-06-29 | v0.39 删除站群导流入口：Footer `Sitios del grupo`、`/showroom/` 站群参考区、全站外链残留审计 | 通过 | 34 个生成页面无站群标题和五个站群 URL 残留，Footer 重新平衡为品牌卡 + 3 个信息列 |
| 第十轮 | 2026-06-29 | v0.40 演示残留清理：联系页发布前说明、酒店案例详情待确认字段、构建与文本审计 | 通过 | 联系页和案例详情不再显示“待确认 / 发布前补充”类半成品口径 |
| 第十一轮 | 2026-06-29 | v0.41 高级 Footer 重设、社媒图标入口、无站群导流复查 | 有条件通过 | 34 个生成页面均含新版报价 CTA 和 6 个社媒入口；社媒账号需上线前业务确认 |
| 第十二轮 | 2026-06-29 | v0.42 首页与资源页 YouTube 内嵌播放器、全屏权限、无外链跳转播放 | 通过 | 首页和 `/recursos/videos/` 均在页面画幅内播放企业视频，旧 `youtu.be` 播放外链已移除 |
| 第十三轮 | 2026-06-29 | v0.43 Hero 卖点区改版：非按钮式图标说明区、移动到 CTA 上方、同步行业/目录/项目页 | 通过 | 15 个重点行业/目录/项目页面均含 4 个非交互式卖点图标，旧 `hero-points` 已清零 |
| 第十四轮 | 2026-06-29 | v0.45 首页 Virtual Showroom 去重：删除左侧重复 featured 卡，四个展厅入口改为主视觉 2x2 图卡 | 通过 | 桌面/平板为 2x2 大图卡，手机横向滑动；4 个 Matterport 卡片完整，无横向溢出和坏图 |
| 第十五轮 | 2026-06-29 | v0.46 Footer `Contacto` 列紧凑排版：图标、字号、行距和 flex 覆盖修复 | 通过 | 联系列不再松散拥挤；桌面/移动端均为 3 条 flex 信息组，无横向溢出和坏图 |
| 第十六轮 | 2026-06-29 | v0.47 老板演示路线自动走查：核心页面、图册弹窗、联系表单、移动菜单、Footer 与半成品文案扫描 | 通过 | 8 条核心路线均 200；图册和联系表单成功反馈正常；手机菜单可见 `Contacto`；无横向溢出、坏图和可见半成品文案 |
| 第十七轮 | 2026-06-29 | v0.49 视觉密度优化第一轮：字号、留白、首页/showroom/footer 文案压缩 | 通过 | 核心页面无横向溢出和坏图，视觉密度下降 |
| 第十八轮 | 2026-06-29 | v0.51 低阻力询盘表单：5 个必填基础字段 + 可选详情折叠 | 通过 | 联系页和图册弹窗表单阻力降低，构建和浏览器检查通过 |
| 第十九轮 | 2026-06-29 | v0.52 全站 34 路由稳定性扫查 | 通过 | 34 路由均 200，无页面级溢出、坏图、半成品残留和核心元素缺失 |
| 第二十轮 | 2026-06-29 | v0.53 正式透明公司 logo 与 favicon 替换 | 通过 | Header/Footer/JSON-LD 使用正式 WebP logo，浏览器标签页使用新公司标识 |
| 第二十一轮 | 2026-06-30 | v0.54 SVG Favicon 替换：使用用户提供的正式 SVG logo | 通过 | Head 优先使用 `/favicon.svg`，PNG 和 Apple Touch Icon fallback 同源生成 |
| 第二十二轮 | 2026-06-30 | v0.55 询盘附件上传入口：显性非必填文件上传字段 | 通过 | 联系页和图册弹窗均可见，不在可选折叠区内，不上传也可提交 |
| 第二十三轮 | 2026-06-30 | v0.56 上传按钮文字可读性修复 | 通过 | 黑色上传按钮内显示西语 `Seleccionar archivos`，文字为白色 |
| 第二十四轮 | 2026-06-30 | v0.57 Footer 顶部大 CTA 删除 | 通过 | 删除页脚顶部重复报价 CTA，保留品牌、社媒、导航和联系信息；桌面/移动端无横向溢出 |
| 第二十五轮 | 2026-06-30 | v0.58 资源子页正式化：Blog 与 Videos 从占位页升级为资源页 | 通过 | 两个资源子页桌面/移动端无横向溢出、坏图和占位残留；Videos 保留内嵌播放器 |
| 第二十六轮 | 2026-06-30 | v0.73 Georgebuild 排版优化、站内 Showroom 弹窗、移动端减密度和字号收敛 | 通过 | 11 条核心路由桌面/移动端无横向溢出；滚动加载后无坏图；Matterport 不再新标签外跳；构建通过 |
| 第二十七轮 | 2026-06-30 | v0.74 Showroom 弹窗焦点管理与素材计划同步 | 通过 | 弹窗打开后焦点进入 `Cerrar`，Esc 关闭后回到原展厅入口；`asset-plan.md` 已同步当前素材状态 |
| 第二十八轮 | 2026-06-30 | v0.75 隐私政策页、表单隐私入口、Footer 隐私入口与 sitemap 收录 | 通过 | 35 个页面构建通过；隐私页桌面/移动端无溢出和坏图；表单、Footer、sitemap 均已接入 |
| 第二十九轮 | 2026-06-30 | v0.76 表单提交结构与成功反馈可访问性 | 通过 | 35 个页面构建通过；联系页表单具备 multipart 结构；提交后成功提示显示并获得焦点 |
| 第三十轮 | 2026-06-30 | v0.77 静态 QA gate 与 Showroom 留站检查 | 通过 | 35 个页面构建通过；`pnpm qa:static` 检查 21 个 Matterport 展厅入口均带站内弹窗标记且无新标签跳转，0 failures / 0 warnings；浏览器点击首页酒店展厅后页面 URL 保持不变并在站内弹窗 iframe 加载 Matterport |
| 第三十一轮 | 2026-06-30 | v0.78 中等桌面 Header 与移动端溢出修复 | 通过 | 1280px 首页 overflow 从 88px 降为 0；390px 首页 overflow 从 16px 降为 0；11 条核心路由桌面/移动端抽查 0 溢出、0 坏图、0 Matterport 外跳、0 残留；构建和静态 QA 均通过 |
| 第三十二轮 | 2026-06-30 | v0.79 项目与联系页视觉密度收敛 | 通过 | `/proyectos/` 项目路线卡片正文压缩，`/contacto/` 辅助说明移动端从 5-6 行收敛到 3-4 行；两页桌面/手机 0 溢出、0 坏图；构建和静态 QA 均通过 |
| 第三十三轮 | 2026-06-30 | v0.80 全站长段落与行内样式收敛 | 通过 | 14 条代表路由桌面/手机抽查 0 溢出、0 坏图、0 超阈值可见长段落；页面/组件旧行内文本样式已清理；构建和静态 QA 均通过 |
| 第三十四轮 | 2026-06-30 | v0.81 Showroom 留站弹窗与 iframe QA 防回退 | 通过 | `pnpm qa:static` 覆盖 21 个 Matterport 展厅入口和 37 个 iframe，0 failures / 0 warnings；首页点击酒店展厅后 URL 保持本站，站内弹窗加载 Matterport，Esc 可关闭并恢复焦点 |
| 第三十五轮 | 2026-06-30 | v0.82 图册下载留资上下文追踪 | 通过 | 71 个图册触发器均带具体 `data-catalog-interest`，图册弹窗标题和隐藏字段同步记录 |
| 第三十六轮 | 2026-06-30 | v0.83 图册 PDF manifest 与映射 QA | 通过 | 21 份本地图册 PDF、41 个前端图册兴趣值映射通过 `pnpm qa:catalogs` |
| 第三十七轮 | 2026-06-30 | v0.84 上线联系方式与表单接收配置闭环 | 有条件通过 | WhatsApp、邮箱、表单 action 可由环境变量配置；真实联系方式和接收端点仍需上线前确认 |
| 第三十八轮 | 2026-06-30 | v0.85-v0.86 真实浏览器 QA gate 与移动端页脚菜单修复 | 通过 | `pnpm qa:browser` 覆盖 11 条核心路由和关键交互；`/fabrica/` 页脚附近打开菜单不再消失 |
| 第三十九轮 | 2026-06-30 | v0.87 首页移动端数据区 2x2 压缩 | 通过 | 390px 首页指标区显示 4 个核心指标、2 列 2 行、0 横向溢出；该布局已纳入 `pnpm qa:browser` 防回退；完整构建、静态 QA、图册 QA、浏览器 QA 均通过 |
| 第四十轮 | 2026-06-30 | v0.88 12 步流程卡片语义图标增强 | 通过 | 每个流程卡片均有对应线性图标；桌面仍为 6 列 2 行，移动端仍为 2 行横向滑动；已纳入 `pnpm qa:browser` 防回退 |
| 第四十一轮 | 2026-06-30 | v0.89 FAQ 分类 Tab + Accordion | 通过 | 首页 FAQ 已改为 4 类分类按钮 + 折叠问答；默认显示第一分类第一问，移动端分类横向滚动；完整构建、静态 QA、图册 QA、浏览器 QA 均通过 |
| 第四十二轮 | 2026-06-30 | v0.90 工厂设备证据链、合作 Logo 两行滚动、FAQ 标题平衡 | 通过 | 首页和 `/fabrica/` 已使用真实设备 WebP 重做工厂表达；合作品牌区改为两行横向滚动；FAQ 标题居中平衡；完整构建、静态 QA、图册 QA、浏览器 QA 均通过 |
| 第四十三轮 | 2026-06-30 | v0.91 横向滑动轨道左右按钮 | 通过 | 主要横向滑动模块已加入明显上一项/下一项圆形按钮；浏览器 QA 验证 5 个代表移动轨道可点击推进且页面横向溢出为 0 |
| 第四十四轮 | 2026-06-30 | v0.92 FAQ 移动端与首页 Showroom 布局回归修复 | 通过 | FAQ 手机端分类按钮改为 2x2 网格；首页 Showroom 桌面端四卡重新铺满内容区；构建、静态 QA、图册 QA、浏览器 QA 均通过 |
| 第四十五轮 | 2026-06-30 | v0.93 Footer 社媒入口与移动端居中 | 通过 | Footer 删除 Pinterest/X，新增 TikTok；手机端品牌区、社媒、导航和联系信息居中；构建、静态 QA、浏览器 QA 均通过 |
| 第四十六轮 | 2026-06-30 | v0.94 上线 QA 社媒防回退 | 通过 | `qa:launch` 已检查 TikTok 存在和 Pinterest/X 移除；本地演示环境按预期仍因真实 WhatsApp、邮箱、表单接收端未配置而失败 |

## 第二轮验收依据

- 构建：`pnpm run build` 已通过，生成 34 个页面，0 errors / 0 warnings / 0 hints。最新复查时间为 2026-06-29 11:09。
- 素材：静态审计确认无缺失图片、无非 WebP 的 Hymueble 页面图片、无缺失 `loading` / `decoding` 属性、无同页重复图片 `src`。
- Logo：公司 logo 已加入全站 Header，Organization JSON-LD 使用生产域名 logo URL。
- 移动端：390px 显示 logo + Hymueble，360px 使用 logo-only 紧凑 Header，均无横向溢出。
- 页面边界：首页已移除过重的 Project Solutions 解释，改为 `Catálogo` / `Proyectos` / `Contacto` 路径桥；完整项目解决方案留在 `/proyectos/`。
- 浏览器点击验收：`/`、`/showroom/`、`/hoteles/`、`/catalogo/hoteleria/`、`/proyectos/hoteleria/`、`/fabrica/`、`/contacto/` 均可打开，无横向溢出，无坏图，保留 WhatsApp 和表单入口。
- 转化交互：酒店图册弹窗可打开、填写并显示成功反馈；联系页主询盘表单可填写并显示成功反馈。
- 移动端菜单：390px 首页 Header 可展开主导航，行业子入口可见。
- 演示证据截图：
  - `output/playwright/v026-route-boundary/v026-home-route-bridge-mobile.png`
  - `output/playwright/v026-route-boundary/v026-proyectos-solutions-mobile.png`
  - `output/playwright/v028-company-logo/v028-logo-desktop.png`
  - `output/playwright/v028-company-logo/v028-logo-mobile-390.png`
  - `output/playwright/v028-company-logo/v028-logo-mobile-360.png`

## 当前未验收或未上线项

- 表单只做前端反馈，不接真实后端。
- WhatsApp 仍使用占位号码，待用户提供真实号码后统一替换。
- 部分案例、证书、图册 PDF 仍需继续入库和精确映射。
- 公司 logo 已替换为用户提供的正式透明 WebP；若后续需要印刷级或品牌手册，可再补 SVG/AI。
- 尚未部署到正式域名，当前只确认本地预览。

## 第二十四轮验收依据

- 构建：`pnpm run build` 已通过，生成 34 个页面，0 errors / 0 warnings / 0 hints。
- Footer：已删除顶部大 CTA 区块，不再显示 `Cierre del proyecto` 和 `¿Listo para cotizar tu proyecto de mobiliario?`。
- 保留内容：品牌信息、社媒图标、解决方案导航、B2B 采购入口和联系入口仍保留；当前 v0.94 社媒集合为 Facebook、Instagram、LinkedIn、YouTube、TikTok，并已纳入上线 QA 防回退。
- 响应式：浏览器 1440px 和 390px 视口确认 `/contacto/` 页面横向溢出为 0，Footer 图片坏图数为 0。
- 截图证据：
  - `output/playwright/v057-footer-cta-removal/contacto-footer-desktop.png`
  - `output/playwright/v057-footer-cta-removal/contacto-footer-mobile.png`
  - `output/playwright/v057-footer-cta-removal/footer-removal-report.json`

## 第二十五轮验收依据

- 构建：`pnpm run build` 已通过，生成 34 个页面，0 errors / 0 warnings / 0 hints。
- 静态扫描：`src/pages/recursos` 与 `src/styles/global.css` 不再含 `.placeholder-page`。
- `/recursos/blog/`：已升级为采购指南资源页，桌面 1440px 和手机 390px 验证横向溢出为 0、坏图为 0、无占位残留文案。
- `/recursos/videos/`：已升级为视觉资源页，桌面 1440px 和手机 390px 验证横向溢出为 0、坏图为 0、无占位残留文案，并保留 1 个内嵌 YouTube iframe。
- 两个页面均保留转化 CTA 与 Showroom 相关入口。
- 截图证据：
  - `output/playwright/v058-resource-subpages/blog-desktop.png`
  - `output/playwright/v058-resource-subpages/blog-mobile.png`
  - `output/playwright/v058-resource-subpages/videos-desktop.png`
  - `output/playwright/v058-resource-subpages/videos-mobile.png`
  - `output/playwright/v058-resource-subpages/resource-subpages-report.json`

## 第二十六轮验收依据

- 构建：`pnpm build` 已通过，38 个 Astro 文件检查，0 errors / 0 warnings / 0 hints，34 个静态页面生成成功。
- Georgebuild 排版方向：已完成首页首屏、通用 Hero、卡片系统、Showroom、Footer、资源页和移动端字号/留白收敛。
- 合作与证书：已删除与项目案例重复的 4 个文字案例卡，改为低密度合作品牌/证书视觉露出和证书 chips。
- 首页 Hero：已删除重复的深色 showroom 浮层卡，右侧保留纯高级场景图；旧 `.showroom-hero-card` CSS 已清理。
- Matterport 展厅：全站展厅入口已改为站内弹窗，未发现 Matterport `target="_blank"` 外跳残留。
- 浏览器自动复检覆盖 11 条核心路由的桌面和移动端：`/`、`/hoteles/`、`/catalogo/`、`/catalogo/hoteleria/`、`/proyectos/`、`/proyectos/hoteleria/`、`/fabrica/`、`/showroom/`、`/recursos/blog/`、`/recursos/videos/`、`/contacto/`。
- 复检结果：页面级横向溢出为 0；滚动加载后坏图为 0；Showroom 弹窗可打开、关闭并清空 iframe。
- 最终烟测：`output/playwright/v073-final-smoke/` 确认首页桌面/移动端无旧 hero showroom 卡、无坏图、无横向溢出；Showroom 弹窗打开时背景滚动锁定，Esc 关闭后 iframe 重置为 `about:blank`。
- 截图与报告证据：
  - `output/playwright/v068-showroom-modal/`
  - `output/playwright/v072-post-density-audit/`
  - `output/playwright/v073-final-smoke/`

## 第二十七轮验收依据

- Showroom 弹窗交互：打开首页任一 `data-showroom-modal` 展厅入口后，焦点自动进入 `Cerrar` 按钮。
- 键盘关闭：按 Esc 后弹窗关闭，iframe `src` 重置为 `about:blank`，背景滚动锁解除。
- 焦点恢复：弹窗关闭后焦点回到原展厅入口，便于键盘用户继续浏览。
- 素材文档：`docs/project-brain/asset-plan.md` 已更新到 2026-06-30，并记录当前生成素材、Matterport 缩略图和站内弹窗交互状态。
- 构建：`pnpm build` 已通过，38 个文件检查，0 errors / 0 warnings / 0 hints，34 个页面生成成功。
- 截图与报告证据：`output/playwright/v074-showroom-focus/`

## 第二十八轮验收依据

- 新增页面：`/privacidad/` 已生成，标题为 `Política de privacidad`，包含 7 个隐私说明内容块。
- 构建：`pnpm build` 已通过，39 个文件检查，0 errors / 0 warnings / 0 hints，35 个页面生成成功。
- 表单入口：联系页和图册弹窗复用的 `ContactForm` 隐私提示已链接到 `/privacidad/`。
- Footer 入口：页脚底部已加入 `/privacidad/` 链接。
- SEO：隐私页 canonical 为 `https://hymueble.com/privacidad/`，sitemap 已收录 `/privacidad/`。
- 浏览器 QA：桌面 1440 x 1000 与手机 390 x 900 均无横向溢出、无坏图。
- 截图与报告证据：`output/playwright/v075-privacy-page/`

## 第二十九轮验收依据

- 构建：`pnpm build` 已通过，39 个文件检查，0 errors / 0 warnings / 0 hints，35 个页面生成成功。
- 表单结构：`/contacto/` 构建产物已确认包含 `method="post"`、`enctype="multipart/form-data"` 和 `data-form-purpose="project-inquiry"`。
- 成功反馈：成功提示已确认包含 `role="status"`、`aria-live="polite"` 和 `tabindex="-1"`。
- 浏览器 QA：在 `/contacto/` 填写 5 个必填字段、选择 `Hoteles` 并提交后，页面停留在 `/contacto/`，`.project-form.is-sent` 生效，`.form-success` 显示且成为当前焦点。
- 边界：表单仍是前端演示反馈，不代表已经接入真实后端或真实附件接收。

## 第三轮验收依据

- 构建：`pnpm run build` 已通过，生成 34 个页面，0 errors / 0 warnings / 0 hints。
- Header：桌面 Header 已移除 `WhatsApp` 文案按钮，只保留 `Showroom` 与 `Cotizar` 两个主要动作。
- 全站 WhatsApp：静态审计确认全部 34 个 HTML 页面均含 `class="whatsapp-float"` 固定绿色 WhatsApp 图标。
- Header 文案：静态审计确认全部 34 个 Header 均含 `Cotizar`，且 Header 内不再含 `>WhatsApp<` 文案按钮。
- Showroom：`/proyectos/hoteleria/`、`/fabrica/`、`/contacto/` 已补直接 Matterport 酒店展厅链接和 `/showroom/` 内链。
- 截图证据：
  - `output/playwright/v032-mobile-showroom/proyectos-hoteleria-mobile.png`
  - `output/playwright/v032-mobile-showroom/fabrica-mobile.png`
  - `output/playwright/v032-mobile-showroom/contacto-mobile.png`
  - `output/playwright/v033-header-whatsapp/header-desktop.png`
  - `output/playwright/v033-header-whatsapp/header-mobile.png`

## 第四轮验收依据

- 构建：`pnpm run build` 已于 2026-06-29 13:11 通过，34 个页面，0 errors / 0 warnings / 0 hints。
- 全站 Header：静态审计确认 34 个页面 Header 均包含 `Showroom` 与 `Cotizar`，且 Header 内不再出现 `WhatsApp` 文案按钮。
- 全站 WhatsApp：静态审计确认 34 个页面均且仅含 1 个 `class="whatsapp-float"` 固定绿色 WhatsApp 图标。
- 链接审计：静态审计覆盖 1570 个内部链接，无缺失目标页面。
- 图片审计：静态审计覆盖 205 张本地图片，无缺失文件、无非 WebP 页面图片、无同页重复图片 `src`，并确认图片均有 `alt`、`loading`、`decoding`。
- 外链资产：静态审计确认当前生成页包含 17 个 Matterport 展厅链接和 2 个 YouTube 链接。

## 第五轮验收依据

- 构建：`pnpm run build` 已通过，生成 34 个页面，0 errors / 0 warnings / 0 hints。
- 流程布局：浏览器 1440px 视口确认 `.process` 含 12 个卡片，首行 6 个卡片，共 2 行，计算列宽约 187px。
- 移动端：浏览器 390px 视口确认 12 个流程卡片保持 1 列，无横向溢出，坏图数为 0。
- 文案：`processSteps` 已精简为短句，保留报价、测量、设计、材料、样板、生产、质检、终检、包装、物流、安装、售后 12 个核心阶段。

## 第六轮验收依据

- 构建：`pnpm run build` 已通过，生成 34 个页面，0 errors / 0 warnings / 0 hints。
- 桌面端：浏览器 1440px 视口确认 12 个流程卡片仍为 6 列 2 行，页面无横向溢出。
- 移动端：浏览器 390px 视口确认 12 个流程卡片为 2 行横向滑动流程带，容器自身可横向滚动，页面整体无横向溢出，坏图数为 0。

## 第七轮验收依据

- 构建：`pnpm run build` 已通过，生成 34 个页面，0 errors / 0 warnings / 0 hints。
- 移动 Header：浏览器 390px 视口确认 `Cotizar` 右边界为 328px、菜单按钮右边界为 376px，均在 390px 视口内，页面横向溢出为 0。
- 移动菜单：浏览器 390px 视口展开后，菜单含 21 个链接，最后 `Contacto` 初始可见，位置约为 top 740px / bottom 772px。
- 菜单容器：展开菜单 `overflow-y: auto`，菜单高度约 718px，适配更矮设备时仍可在菜单内部滚动。

## 第八轮验收依据

- 构建：`pnpm run build` 已通过，生成 34 个页面，0 errors / 0 warnings / 0 hints。
- 页脚结构：全站 Footer 改为上方项目询盘 CTA + 下方品牌/行业/采购/能力信息区。
- 站群入口：v0.38 曾参考用户提供的站群资料加入外链入口；v0.39 已按用户反馈移除，当前不再作为站内导流入口。
- 素材：页脚背景使用本地 WebP 视觉素材，页脚内 logo 图片加载正常，页脚自身坏图数为 0。
- 响应式：浏览器 1440px 和 390px 视口确认页脚无横向溢出，390px 视口页脚信息区自动变为单列。
- 截图证据：
  - `output/playwright/v038-footer-redesign/footer-desktop.png`
  - `output/playwright/v038-footer-redesign/footer-mobile.png`

## 第九轮验收依据

- 构建：`pnpm run build` 已通过，生成 34 个页面，0 errors / 0 warnings / 0 hints。
- Footer：`src/components/Footer.astro` 已删除 `Sitios del grupo` 整列，页脚桌面布局调整为品牌卡 + `Sectores` / `Compra B2B` / `Capacidades` 三列。
- Showroom：`/showroom/` 页面已删除 `Sitios especializados del grupo` 站群参考区。
- 全站静态审计：生成后的 34 个 HTML 页面均不含 `Sitios del grupo`、`Sitios especializados del grupo`，且不含酒店、办公、医疗、教育、住宅五个站群 URL。
- 浏览器验收：1440px 和 390px 视口均确认 Footer 无站群标题、无站群链接、无横向溢出、页脚图片无断图。
- 截图证据：
  - `output/playwright/v039-footer-no-group-links/footer-desktop.png`
  - `output/playwright/v039-footer-no-group-links/footer-mobile.png`

## 第十轮验收依据

- 构建：`pnpm run build` 已通过，生成 34 个页面，0 errors / 0 warnings / 0 hints。
- 联系页：`/contacto/` 的生产基地说明已从“发布前补充地址/法律信息”改为正式商业表达。
- 案例详情：Grand Kempinski、Hilton DoubleTree、Raffles 的交付字段已从“年份和房间数待确认”改为“Referencia documentada con material interno de proyecto”，不编造年份和房间数，也不暴露半成品状态。
- 文本审计：源码与生成页均不含 `pendiente de confirmar`、`antes de publicar`、`datos legales`、`dirección final`、`Vista previa`、`validación comercial`、`Información final`。
- 浏览器抽检：390px 联系页和 1440px Kempinski 案例详情页无横向溢出，且未出现上述演示残留文本。
- 截图证据：
  - `output/playwright/v040-demo-residue-cleanup/contacto-mobile.png`
  - `output/playwright/v040-demo-residue-cleanup/kempinski-desktop.png`

## 第十一轮验收依据

- 构建：`pnpm run build` 已通过，生成 34 个页面，0 errors / 0 warnings / 0 hints。
- Footer：全站 Footer 已从旧信息列样式升级为高级转化收尾区，包含项目报价大 CTA、商业响应面板、Showroom 入口、品牌说明、导航、联系信息和社媒图标。
- 社媒入口：v0.41 当时接入 Facebook、Instagram、LinkedIn、YouTube、Pinterest、X；当前已由 v0.93 调整为 Facebook、Instagram、LinkedIn、YouTube、TikTok，并且每个入口均使用图标按钮呈现。
- 站群导流：静态审计确认生成后的 34 个 HTML 页面均不含 `Sitios del grupo`、`Sitios especializados del grupo`，也不含五个站群 URL。
- 浏览器验收：1440px 和 390px 视口均确认 Footer 无横向溢出，页脚图片无断图，社媒图标可见。
- 截图证据：
  - `output/playwright/v041-footer-premium-social/footer-desktop.png`
  - `output/playwright/v041-footer-premium-social/footer-mobile.png`

### 第十一轮剩余说明

- 社媒链接来自公开公司资料源；正式上线前建议由用户确认是否全部作为 Hymueble 对外账号使用。
- TikTok 已在 v0.93 接入 `https://www.tiktok.com/@hongyefurniture_`，正式上线前仍需用户确认该账号是否作为 Hymueble 对外账号使用。

## 第十二轮验收依据

- 构建：`pnpm run build` 已通过，生成 34 个页面，0 errors / 0 warnings / 0 hints。
- 首页：`/` 的企业视频模块已从封面外链改为右侧 16:9 YouTube 内嵌播放器。
- 资源视频页：`/recursos/videos/` 已同步使用同一 YouTube 内嵌播放器，避免一个入口内嵌、另一个入口跳外链的不一致体验。
- 全屏能力：静态审计与浏览器检查确认 iframe 含 `allowfullscreen`，且 `allow` 权限包含 `fullscreen`。
- 外链跳转：静态审计确认首页和 `/recursos/videos/` 不再含旧 `youtu.be/a1ECkth6WnQ` 播放外链。
- 浏览器验收：1440px 首页、390px 首页、1440px 视频页均确认视频 iframe 在画幅内，页面横向溢出为 0。
- 截图证据：
  - `output/playwright/v042-youtube-embed/home-desktop.png`
  - `output/playwright/v042-youtube-embed/home-mobile.png`
  - `output/playwright/v042-youtube-embed/videos-desktop.png`

## 第十三轮验收依据

- 构建：`pnpm run build` 已通过，生成 34 个页面，0 errors / 0 warnings / 0 hints。
- 结构：`Hero.astro` 已将原 `hero-points` 卖点卡片替换为 `ValuePoints.astro` 图标说明区。
- 位置：浏览器检查确认新卖点区位于 lead 文案下方、CTA 按钮上方。
- 非交互：浏览器检查确认新卖点区内没有 `<a>` 或 `<button>`，不会被误认为可点击 CTA。
- 覆盖：静态审计确认 5 个行业页、5 个目录页、5 个项目页均包含 4 个 `value-point`，且全站生成 HTML 无旧 `hero-points` 残留。
- 响应式：1440px 和 390px 酒店页均无横向溢出、无坏图；移动端新卖点区自动变为单列。
- 截图证据：
  - `output/playwright/v043-value-points/hoteles-desktop.png`
  - `output/playwright/v043-value-points/hoteles-mobile.png`
  - `output/playwright/v043-value-points/catalogo-hoteleria-desktop.png`
  - `output/playwright/v043-value-points/proyectos-hoteleria-desktop.png`

## 第十四轮验收依据

- 构建：`pnpm run build` 已通过，生成 34 个页面，0 errors / 0 warnings / 0 hints。
- 首页 Showroom：`/` 已删除左侧重复的酒店 featured 展厅卡，保留 `Hoteles`、`Oficinas`、`Salud`、`Educación` 四个展厅入口作为主视觉内容。
- 链接：浏览器检查确认 4 个展厅卡片均指向 Matterport 链接，模块内保留 `/showroom/` 总入口和 `/contacto/` 需求提交入口。
- 响应式：1440px 桌面和 900px 平板为 2x2 大图卡；390px 手机为横向滑动卡片，页面整体横向溢出为 0。
- 图片：浏览器检查确认 4 个展厅卡片均含图片，坏图数为 0。
- 截图证据：
  - `output/playwright/v045-showroom-single-column/showroom-desktop.png`
  - `output/playwright/v045-showroom-single-column/showroom-tablet.png`
  - `output/playwright/v045-showroom-single-column/showroom-mobile.png`

## 第十五轮验收依据

- 构建：`pnpm run build` 已通过，生成 34 个页面，0 errors / 0 warnings / 0 hints。
- Footer：`Contacto` 列链接恢复为 `display: flex` 信息组，避免被普通 footer 列链接样式覆盖成 `display: block`。
- 视觉参数：联系列链接字号为 15px，行高约 20.7px；图标尺寸为 30px，视觉密度比上一版更紧凑。
- 响应式：1440px 桌面与 390px 手机视口均确认 Footer 横向溢出为 0，页面横向溢出为 0。
- 图片：Footer logo 图片加载正常，坏图数为 0。
- 截图证据：
  - `output/playwright/v046-footer-contact-compact/footer-desktop.png`
  - `output/playwright/v046-footer-contact-compact/footer-mobile.png`

## 第十六轮验收依据

- 构建：`pnpm run build` 已通过，生成 34 个页面，0 errors / 0 warnings / 0 hints。
- 工具前置：`npx` 可用；浏览器走查使用本机 Google Chrome 执行。
- 演示路线：`/`、`/showroom/`、`/hoteles/`、`/catalogo/hoteleria/`、`/proyectos/hoteleria/`、`/proyectos/hoteleria/hilton-doubletree-hotel/`、`/fabrica/`、`/contacto/` 均返回 200。
- 页面基础：上述 8 条路线均无页面级横向溢出、无坏图，均保留 Header `Showroom` / `Cotizar`、右下角 WhatsApp 浮动入口、Footer 社媒入口和 3 条 flex 联系信息组。
- 可见文案：8 条路线均未出现 `待确认`、`发布前`、`placeholder`、`local preview`、`pendiente de confirmar`、`antes de publicar`、`Vista previa`、`Información final`。
- 图册弹窗：`/catalogo/hoteleria/` 可打开图册弹窗，填写演示项目资料后显示西语成功反馈。
- 联系表单：`/contacto/` 主表单可填写演示项目资料并显示西语成功反馈。
- 移动菜单：390px 首页展开菜单后，21 个导航链接可用，最终 `Contacto` 链接初始可见，页面横向溢出为 0。
- 截图与报告证据：
  - `output/playwright/v047-boss-demo-walkthrough/boss-demo-walkthrough-report.json`
  - `output/playwright/v047-boss-demo-walkthrough/route-home.png`
  - `output/playwright/v047-boss-demo-walkthrough/route-showroom.png`
  - `output/playwright/v047-boss-demo-walkthrough/catalog-modal-success.png`
  - `output/playwright/v047-boss-demo-walkthrough/contact-form-success.png`
  - `output/playwright/v047-boss-demo-walkthrough/mobile-menu-open.png`

## 第十七轮验收依据

- 范围：v0.49 视觉密度优化第一轮代码落地。
- 构建：`pnpm run build` 已通过，37 个 Astro/TS 文件检查为 0 errors / 0 warnings / 0 hints，静态生成 34 个页面。
- 字号与密度：
  - 首页 H1 桌面端为 56px，移动端为 38px。
  - 首页普通 H2 样本桌面端收在 40px，移动端收在 30px。
  - 首页 Showroom 作为重点体验模块保留 48px H2。
  - 联系页 H1 从 56px 上限收紧到 50px。
  - 质量控制证明卡和检查点文案已精简为短句。
- 页面基础：
  - `/` 桌面和移动端均为 0 坏图、0 页面级横向溢出。
  - `/showroom/` 桌面端为 0 坏图、0 页面级横向溢出。
  - `/contacto/` 桌面端为 0 坏图、0 页面级横向溢出。
  - 首页 390px 移动菜单展开后，`Contacto` 可见，页面级横向溢出为 0。
- 功能数量未回退：首页保留 4 个 Showroom 卡、4 个质量证明卡、6 个质量检查点；Footer 当前保留 5 个社媒入口。
- 截图与报告证据：
  - `output/playwright/v049-visual-density-polish/visual-density-report.json`
  - `output/playwright/v049-visual-density-polish/home-desktop.png`
  - `output/playwright/v049-visual-density-polish/home-mobile.png`
  - `output/playwright/v049-visual-density-polish/showroom-desktop.png`
  - `output/playwright/v049-visual-density-polish/contacto-desktop.png`
  - `output/playwright/v049-visual-density-polish/mobile-menu-open.png`

## 第十八轮验收依据

- 范围：v0.51 低阻力询盘表单。
- 构建：`pnpm run build` 已通过，37 个文件检查为 0 errors / 0 warnings / 0 hints，静态生成 34 个页面。
- 表单策略：首屏表单从长问卷改为快速询盘，只保留 5 个必填基础字段：
  - `company`
  - `whatsapp`
  - `location`
  - `sector`
  - `message`
- 可选字段：`email`、`spaces`、`quantity`、`stage`、`timeline`、`budget` 仍保留，但进入默认关闭的 `Agregar detalles opcionales` 展开区。
- 联系页验证：
  - `/contacto/` 1440 x 1100：0 坏图、0 页面级横向溢出。
  - `/contacto/` 390 x 1100：0 坏图、0 页面级横向溢出。
  - 表单提示显示 `Solo necesitamos 5 datos para iniciar.`，降低首次询盘心理阻力。
- 图册弹窗验证：
  - `/catalogo/hoteleria/` 弹窗可打开。
  - 弹窗表单同样只要求 5 个基础必填字段。
  - 可选详情默认关闭。
  - 0 坏图、0 页面级横向溢出。
- 截图与报告证据：
  - `output/playwright/v051-low-friction-form/low-friction-form-report.json`
  - `output/playwright/v051-low-friction-form/contacto-desktop.png`
  - `output/playwright/v051-low-friction-form/contacto-mobile.png`
  - `output/playwright/v051-low-friction-form/catalog-modal-open.png`

## 第十九轮验收依据

- 范围：v0.52 全站 34 路由浏览器稳定性扫查。
- 构建：`pnpm run build` 已通过，37 个文件检查为 0 errors / 0 warnings / 0 hints，静态生成 34 个页面。
- 路由状态：34 个内部页面全部返回 200，无坏状态页面。
- 页面基础：
  - 0 个页面级横向溢出路由。
  - 0 个坏图路由。
  - 0 个可见半成品残留文案路由。
  - 0 个非 WebP Hymueble 图片路由。
  - 0 个核心 Header / Footer / WhatsApp 元素缺失路由。
  - 0 个内部链接格式异常路由。
- 移动菜单：390 x 844 视口下菜单可展开，21 个链接可用，`Contacto` 可见。
- 截图与报告证据：
  - `output/playwright/v052-full-route-sweep/full-route-sweep-report.json`
  - `output/playwright/v052-full-route-sweep/mobile-menu-open.png`

## 第二十轮验收依据

- 范围：v0.53 正式透明公司 logo 与 favicon 替换。
- 构建：`pnpm run build` 已通过，37 个文件检查为 0 errors / 0 warnings / 0 hints，静态生成 34 个页面。
- Logo：
  - Header logo 已使用用户提供的正式透明 WebP 源文件，路径保持为 `/assets/hymueble/brand/hongye-shengda-logo.webp`。
  - Footer logo 已同步使用同一正式 WebP 源文件。
  - Organization JSON-LD 继续通过 `brand.logo` 指向同一正式 logo 路径。
- Favicon：
  - 浏览器标签页 favicon 已从旧 `H` 图标替换为正式 logo 源图裁切的中心标识。
  - Head 中已接入 `/favicon.webp`、`/favicon.png` 和 `/apple-touch-icon.png`。
- 浏览器验证：
  - `/` 1440 x 900：Header/Footer logo 加载正常，页面级横向溢出为 0。
  - `/` 390 x 844：Header logo 与 `Cotizar`、菜单按钮不冲突，页面级横向溢出为 0。
  - `/contacto/` 1440 x 900：Header/Footer logo 加载正常，页面级横向溢出为 0。
  - `/` 与 `/contacto/` 滚动复查后无确认坏图。
- 截图与报告证据：
  - `output/playwright/v053-logo-replacement/logo-replacement-report.json`
  - `output/playwright/v053-logo-replacement/home-desktop.png`
  - `output/playwright/v053-logo-replacement/home-mobile.png`
  - `output/playwright/v053-logo-replacement/contacto-desktop.png`

## 第二十一轮验收依据

- 范围：v0.54 SVG Favicon 替换。
- 输入文件：`/Users/yoyo/Downloads/hongye-logo-transparent.svg`。
- 构建：`pnpm run build` 已通过，37 个文件检查为 0 errors / 0 warnings / 0 hints，静态生成 34 个页面。
- Favicon：
  - `public/favicon.svg` 已使用用户提供的正式 SVG logo，不再使用上一版裁切图标。
  - `public/favicon.png`、`public/favicon.webp`、`public/apple-touch-icon.png` 已从同一 SVG 内嵌图像重新生成。
  - 页面 head 优先使用 `<link rel="icon" href="/favicon.svg" type="image/svg+xml">`。
  - PNG fallback 和 Apple Touch Icon 仍保留。
- 浏览器验证：
  - `/contacto/` 1440 x 900：`/favicon.svg` 返回 200，content-type 为 `image/svg+xml`。
  - `/favicon.png` 和 `/apple-touch-icon.png` 均返回 200，content-type 为 `image/png`。
  - 页面级横向溢出为 0。
  - 确认坏图数为 0。
- 截图与报告证据：
  - `output/playwright/v054-svg-favicon/svg-favicon-report.json`
  - `output/playwright/v054-svg-favicon/contacto-favicon-check.png`

## 第二十二轮验收依据

- 范围：v0.55 询盘附件上传入口。
- 构建：`pnpm run build` 已通过，37 个文件检查为 0 errors / 0 warnings / 0 hints，静态生成 34 个页面。
- 表单策略：
  - 附件字段为显性展示，不放入 `Agregar detalles opcionales`。
  - 附件字段不设为必填，客户不上传图纸或清单也可以提交。
  - 字段支持多文件选择。
  - 支持 PDF、Word、Excel、CSV、图片、ZIP/RAR、DWG/DXF 等项目资料格式。
  - 原生英文文件按钮已替换为西语自定义控件：`Seleccionar archivos` / `Ningún archivo seleccionado`。
- 联系页验证：
  - `/contacto/` 1440 x 1100：附件字段存在且可见，不是必填，不在可选折叠区内，页面级横向溢出为 0，确认坏图数为 0。
  - `/contacto/` 390 x 1000：附件字段存在且可见，不是必填，不在可选折叠区内，页面级横向溢出为 0，确认坏图数为 0。
  - 不上传附件时填写 5 个基础必填字段，表单仍显示成功反馈。
- 图册弹窗验证：
  - `/catalogo/hoteleria/` 弹窗打开后，附件字段存在且可见。
  - 附件字段不是必填，不在可选折叠区内。
  - 弹窗打开状态页面级横向溢出为 0。
- 截图与报告证据：
  - `output/playwright/v055-visible-attachment-field/attachment-field-report.json`
  - `output/playwright/v055-visible-attachment-field/contacto-desktop-custom.png`
  - `output/playwright/v055-visible-attachment-field/contacto-mobile-custom.png`
  - `output/playwright/v055-visible-attachment-field/catalog-modal-attachment-custom.png`

## 第二十三轮验收依据

- 范围：v0.56 上传按钮文字可读性修复。
- 构建：`pnpm run build` 已通过，37 个文件检查为 0 errors / 0 warnings / 0 hints，静态生成 34 个页面。
- 修复内容：
  - 上传区标题 label 样式已收窄为 `.file-upload > label`。
  - 上传按钮样式已加强为 `.file-upload-control .file-upload-button`。
  - 按钮继续使用西语文案 `Seleccionar archivos`。
- 浏览器验证：
  - `/contacto/` 390 x 1000：按钮文字为 `Seleccionar archivos`。
  - 空文件状态为 `Ningún archivo seleccionado`。
  - 按钮计算文字颜色为 `rgb(255, 255, 255)`。
  - 按钮计算背景色为 `rgb(18, 26, 34)`。
  - 页面级横向溢出为 0。
- 截图与报告证据：
  - `output/playwright/v056-upload-button-text/upload-button-text-report.json`
  - `output/playwright/v056-upload-button-text/contacto-mobile-upload-button.png`

## 第三十五轮验收依据

- 范围：v0.82 图册下载留资上下文追踪。
- 构建：`pnpm build` 已通过，静态生成 35 个页面。
- 静态 QA：`pnpm qa:static` 已通过，覆盖 35 个 HTML、35 个 sitemap 路由、1,636 个内部链接、418 个本地资产、243 张图片、21 个 Matterport 展厅入口、37 个 iframe、71 个图册触发器和 36 个表单，0 failures / 0 warnings。
- 图册触发器：
  - 所有 `data-download-catalog` 触发器必须带 `data-catalog-interest`。
  - 触发来源覆盖图册枢纽、酒店图册页、酒店分类页、动态行业图册页和工厂页。
- 浏览器验证：
  - `/catalogo/hoteleria/` 点击 `Solicitar catálogo PDF` 后，URL 仍为 `http://127.0.0.1:4321/catalogo/hoteleria/`。
  - 图册弹窗标题显示 `Solicitar catálogo: Catálogo hotelero`。
  - 表单隐藏字段 `catalog_interest` 的值为 `Catálogo hotelero`。
- 仍未完成：
  - 真实 PDF 发送、CRM/邮箱/表单服务接收和附件真实上传仍需上线前接入。
- 报告证据：
  - `output/playwright/v082-catalog-lead-context/report.json`

## 第三十六轮验收依据

- 范围：v0.83 图册 PDF 源文件 manifest 与映射 QA。
- 构建：`pnpm build` 已通过，41 个文件检查为 0 errors / 0 warnings / 0 hints，静态生成 35 个页面。
- 静态 QA：`pnpm qa:static` 已通过，覆盖 35 个 HTML、35 个 sitemap 路由、1,636 个内部链接、418 个本地资产、243 张图片、21 个 Matterport 展厅入口、37 个 iframe、71 个图册触发器和 36 个表单，0 failures / 0 warnings。
- 图册源文件 QA：`pnpm qa:catalogs` 已通过。
  - 21 份 PDF 源文件存在。
  - 行业覆盖：hoteleria 2、oficinas 6、salud 3、educacion 3、residencial 7。
  - 页面渲染出的 41 个唯一 `data-catalog-interest` 均能映射到 manifest。
  - PDF 源文件合计约 754.7MB。
  - failures: 0。
  - warnings: 0。
- 策略：
  - 当前不把大 PDF 直接放到 `public/`。
  - 图册仍采用留资优先策略，后续由表单服务、CRM 或销售流程按 `catalog_interest` 发送对应 PDF 或受控链接。
- 报告证据：
  - `docs/project-brain/04-素材/catalog-pdf-manifest.json`
  - `output/qa-catalog-sources-report.json`
  - `output/playwright/v083-catalog-pdf-manifest/report.json`

## 第三十七轮验收依据

- 范围：v0.84 上线联系方式与表单接收配置闭环。
- 本地演示模式验证：
  - `pnpm build` 已通过，42 个文件检查为 0 errors / 0 warnings / 0 hints，静态生成 35 个页面。
  - `pnpm qa:static` 已通过，覆盖 35 个 HTML、35 个 sitemap 路由、1,636 个内部链接、418 个本地资产、243 张图片、21 个 Matterport 展厅入口、37 个 iframe、71 个图册触发器和 36 个表单，0 failures / 0 warnings。
  - `pnpm qa:catalogs` 已通过，21 份 PDF、41 个图册兴趣值映射，0 failures / 0 warnings。
- 上线配置模式验证：
  - 使用非真实测试环境变量完成一次构建。
  - `pnpm qa:launch` 已通过。
  - 生成 HTML 中 demo forms：0。
  - 生成 HTML 中占位 WhatsApp 链接：0。
- 配置项：
  - `PUBLIC_HYMUEBLE_WHATSAPP_URL`
  - `PUBLIC_HYMUEBLE_EMAIL`
  - `PUBLIC_HYMUEBLE_FORM_ACTION`
- 仍未完成：
  - 真实 WhatsApp、真实业务邮箱和真实表单接收端点仍需用户确认。
- 报告证据：
  - `docs/project-brain/05-开发/07-环境变量与上线配置.md`
  - `output/playwright/v084-launch-contact-config/report.json`

## 第三十八轮验收依据

- 范围：v0.85-v0.86 真实浏览器 QA gate 与移动端页脚菜单修复。
- 构建：`pnpm build` 已通过，43 个文件检查为 0 errors / 0 warnings / 0 hints，静态生成 35 个页面。
- 静态 QA：`pnpm qa:static` 已通过，覆盖 35 个 HTML、35 个 sitemap 路由、1,636 个内部链接、418 个本地资产、243 张图片、21 个 Matterport 展厅入口、37 个 iframe、71 个图册触发器和 36 个表单，0 failures / 0 warnings。
- 图册 QA：`pnpm qa:catalogs` 已通过，21 份 PDF、41 个图册兴趣值映射，0 failures / 0 warnings。
- 浏览器 QA：`pnpm qa:browser` 已通过，覆盖 11 条核心路由的桌面与移动端基础状态，并验证移动菜单、`/fabrica/` 页脚附近菜单、Showroom 弹窗、图册弹窗和联系表单演示反馈。
- 移动菜单修复结果：
  - `/fabrica/` 手机视口滚动到页脚附近后点击汉堡菜单，Header 仍固定在视口顶部。
  - 菜单面板从 Header 下方展开，`Contacto` 链接可见。
  - 页面级横向溢出为 0。
- 报告证据：
  - `output/playwright/v085-browser-demo-walkthrough/report.json`
  - `output/playwright/v085-browser-demo-walkthrough/mobile-fabrica-footer-menu.png`
  - `output/playwright/v086-mobile-menu-footer-after.png`

## 第三十九轮验收依据

- 范围：v0.87 首页移动端信任数据区 2 行 2 列压缩。
- 用户需求：把移动端长列表式数据区改为“两行两列”，降低移动端视觉长度和阅读压力。
- 实现口径：
  - 移动端保留 4 个核心指标：`40+`、`350.000 m²`、`40.000 m²`、`10.000+`。
  - 第 5 个 `2.000+ personas dentro del grupo` 仅在移动端隐藏，桌面端仍保留。
  - 移动端指标数字字号收敛，避免 `350.000 m²` 在 178px 卡片内被截断。
- 浏览器验证：
  - 390px 视口下 `.metric-grid` 为两列：`178px 178px`。
  - 可见指标数量为 4。
  - 页面级横向溢出为 0。
  - `pnpm qa:browser` 已新增 `mobile metrics grid` 检查：2 columns、4 visible metrics、expectedRows true、pageOverflow 0。
- 构建与 QA：
  - `pnpm build` 已通过，43 个文件检查为 0 errors / 0 warnings / 0 hints，静态生成 35 个页面。
  - `pnpm qa:static` 已通过，0 failures / 0 warnings。
  - `pnpm qa:catalogs` 已通过，0 failures / 0 warnings。
  - `pnpm qa:browser` 已通过，0 failures / 0 warnings。
- 截图证据：
  - `output/playwright/v087-mobile-metrics-grid.png`

## 第四十轮验收依据

- 范围：v0.88 12 步项目流程卡片图标增强。
- 用户需求：在每个数字附近加入图标，按每个小模块文字匹配图标，增加视觉趣味性。
- 实现口径：
  - `Cotización`：报价/清单图标。
  - `Medición en sitio`：测量图标。
  - `Diseño técnico`：图纸/设计文档图标。
  - `Acabados`：材料/饰面图标。
  - `Muestra o piloto`：样品箱图标。
  - `Producción`：生产工位图标。
  - `Control de calidad`：盾牌勾选图标。
  - `Inspección final`：最终检查图标。
  - `Embalaje`：包装箱图标。
  - `Logística`：卡车图标。
  - `Instalación`：安装/现场图标。
  - `Postventa`：售后支持图标。
- 浏览器验证：
  - 桌面 1440px：12 个流程卡、12 个图标、2 行、页面级横向溢出 0。
  - 移动 390px：12 个流程卡、12 个图标、2 行横向滑动、页面级横向溢出 0。
  - `pnpm qa:browser` 已新增 `desktop process icons` 和 `mobile process icons` 检查。
- 构建与 QA：
  - `pnpm build` 已通过。
  - `pnpm qa:static` 已通过。
  - `pnpm qa:catalogs` 已通过。
  - `pnpm qa:browser` 已通过。
- 截图证据：
  - `output/playwright/v088-process-icons-desktop.png`
  - `output/playwright/v088-process-icons-mobile.png`

## 第四十一轮验收依据

- 范围：v0.89 首页 FAQ 分类 Tab + Accordion 改版。
- 用户需求：把当前 FAQ 改为“分类 Tab + 折叠问答 Accordion”，并使用用户提供的最终西语 FAQ 文案。
- 实现口径：
  - FAQ 数据集中在 `src/data/site.ts` 的 `faqCategories` 中维护。
  - 每个分类对象包含 `label` 和 `questions`。
  - 每个问题对象包含 `question` 和 `answer`。
  - `src/components/FAQTabs.astro` 负责渲染 Tab、tabpanel、问题按钮和答案区域。
  - 默认选中 `Sobre productos y fabricación`，并默认展开 `¿Fabrican a medida o solo tienen productos estándar de catálogo?`。
  - 点击分类后只展示当前分类的问题。
  - 同一分类内一次只展开一个问题，减少阅读干扰。
  - 移动端分类按钮横向滚动，不挤压变形。
- 浏览器验证：
  - 桌面 1440px：首页 FAQ 分类数为 4，面板数为 4，页面级横向溢出 0。
  - 默认选中分类为 `Sobre productos y fabricación`。
  - 默认展开问题为 `¿Fabrican a medida o solo tienen productos estándar de catálogo?`。
  - 点击 `Sobre logística e importación` 后，面板切换成功，并展开该分类第一问。
  - 点击 `¿Quién se encarga de la importación en mi país?` 后，对应答案可见，且当前分类内只保留 1 个展开项。
  - 移动 390px：首页页面级横向溢出 0，FAQ 分类按钮内部横向滚动。
- 构建与 QA：
  - `pnpm build` 已通过。
  - `pnpm qa:static` 已通过。
  - `pnpm qa:catalogs` 已通过。
  - `QA_BASE_URL=http://127.0.0.1:4322 pnpm qa:browser` 已通过，0 failures / 0 warnings。
- 截图证据：
  - `output/playwright/v085-browser-demo-walkthrough/desktop-home-faq-tabs.png`
  - `output/playwright/v085-browser-demo-walkthrough/mobile-home-faq-tabs.png`
  - `output/playwright/v085-browser-demo-walkthrough/report.json`

## 第四十二轮验收依据

- 范围：v0.90 工厂设备证据链、合作品牌 Logo 区视觉比例、FAQ 标题视觉平衡。
- 用户需求：
  - 根据新增“酒店官网生产设备和工序资料”重写首页 Fábrica 模块和 `/fabrica/` 页面，展示真材实料的工厂实力。
  - 不再重复使用椅子车间图，每个系列优先选择机器设备照片。
  - 合作伙伴 Logo 区改成两行加滚动，降低奇怪视觉占比。
  - FAQ 三个字的位置重新平衡。
- 实现口径：
  - 新增真实设备 WebP：HOMAG 封边、Bacci 五轴 CNC、六面钻、数控板式加工、无尘/水性喷涂、质检包装。
  - 首页 Fábrica 模块改为 `FactoryEvidence`，用大图 + 设备证据卡 + 低密度能力条呈现。
  - `/fabrica/` 改为设备证据页，使用 `FactoryProcessEvidence` 展示 6 个关键设备/工序，删除重复展厅表达。
  - 合作品牌 rail 改为 2 行横向滚动，桌面与移动端均避免页面级横向溢出。
  - 首页 FAQ 区标题改为居中结构，保留原有 FAQTabs 交互和最终西语文案。
- 浏览器验证：
  - 首页工厂区使用新增设备 WebP，未使用用户要求避免重复的椅子车间图。
  - `/fabrica/` 工序区使用 6 张新增设备/工序 WebP。
  - 合作品牌 rail 桌面为两行，移动端仍为两行内部横向滚动，页面级横向溢出为 0。
  - FAQ 标题与说明居中，FAQ Tab + Accordion 交互保持可用，桌面和移动端页面级横向溢出为 0。
- 构建与 QA：
  - `pnpm build` 已通过。
  - `pnpm qa:static` 已通过。
  - `pnpm qa:catalogs` 已通过。
  - `pnpm qa:browser` 已通过。
- 截图证据：
  - `output/playwright/v090-factory-evidence/home-factory-desktop.png`
  - `output/playwright/v090-factory-evidence/home-factory-mobile.png`
  - `output/playwright/v090-factory-evidence/fabrica-hero-desktop.png`
  - `output/playwright/v090-factory-evidence/fabrica-process-desktop.png`
  - `output/playwright/v090-factory-evidence/fabrica-process-mobile.png`
  - `output/playwright/v090-factory-evidence/trust-partners-desktop.png`
  - `output/playwright/v090-factory-evidence/trust-partners-mobile.png`
  - `output/playwright/v090-factory-evidence/faq-desktop.png`
  - `output/playwright/v090-factory-evidence/faq-mobile.png`

## 第四十三轮验收依据

- 范围：v0.91 横向滑动轨道左右按钮机制。
- 用户需求：把现有滑动轨道模块调整为带左右前进/后退按钮的机制，避免用户忽略细小滚动条；不模仿参考图的卡片摆放，只借鉴明显按钮机制。
- 实现口径：
  - 新增 `src/components/ScrollRail.astro`，提供统一上一项/下一项圆形按钮。
  - `BaseLayout` 统一绑定滚动控制脚本：自动检测轨道是否可滚动，只有存在内部横向溢出时显示按钮；到达起点或终点时禁用对应按钮。
  - 保持原卡片排列和内容不变，只增强现有轨道的可发现性和可操作性。
  - 已接入首页 Showroom、首页项目卡、共享流程步骤、合作品牌、证书图、首页工厂设备证据、`/fabrica/` 工序卡和资质卡。
- 浏览器验证：
  - 移动端合作品牌 rail：内部溢出 872px，点击下一项后 `scrollLeft=220`，页面横向溢出 0。
  - 移动端项目卡 rail：内部溢出 812px，点击下一项后 `scrollLeft=296`，页面横向溢出 0。
  - 移动端流程步骤 rail：内部溢出 675px，点击下一项后 `scrollLeft=279`，页面横向溢出 0。
  - 移动端工厂工序 rail：内部溢出 466px，点击下一项后 `scrollLeft=282`，页面横向溢出 0。
  - 移动端工厂资质 rail：内部溢出 480px，点击下一项后 `scrollLeft=284`，页面横向溢出 0。
- 构建与 QA：
  - `pnpm build` 已通过。
  - `pnpm qa:static` 已通过。
  - `pnpm qa:browser` 已通过，0 failures / 0 warnings。
- 证据：
  - `output/playwright/v085-browser-demo-walkthrough/report.json`

## 第四十四轮验收依据

- 范围：v0.92 FAQ 移动端与首页 Showroom 桌面端布局回归修复。
- 用户反馈：
  - FAQ 移动端标题、说明和分类按钮区域出现横向错位与裁切。
  - 首页 Showroom 区四个展厅卡片挤在左侧，右侧留空，桌面视觉不平衡。
- 实现口径：
  - FAQ 移动端不再使用负外边距横向滚动分类按钮，改为 2x2 自适应按钮网格。
  - FAQ 文案、分类、Accordion 交互均保持不变。
  - 首页 Showroom 中 `ScrollRail` 外壳跨满 `.showroom-copy` 的两列网格，四张展厅卡重新占满桌面内容区。
  - Showroom 轨道无内部溢出时继续自动隐藏左右按钮。
- 浏览器验证：
  - 390px 移动端 FAQ 页面级横向溢出为 0。
  - 390px 移动端 FAQ 分类按钮内部溢出为 0，四个分类按钮完整可见。
  - 点击 `Sobre logística e importación` 后 FAQ 切换正常，展开项保持可用。
  - 2048px 桌面端首页 Showroom track 宽度为 1280px，四个卡片列宽均为 305px，轨道溢出为 0。
  - 桌面端 Showroom 左右按钮处于隐藏状态，因为四张卡完整展示无需滚动。
- 构建与 QA：
  - `pnpm build` 已通过。
  - `pnpm qa:static` 已通过。
  - `pnpm qa:catalogs` 已通过。
  - `pnpm qa:browser` 已通过，0 failures / 0 warnings。
