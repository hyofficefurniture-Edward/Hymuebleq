# 当前状态

- 状态：进行中
- 最后更新：2026-07-02
- 负责人：主对话总控
- 关联资料：全部项目资料
- 关联页面：全站
- 下一步：整理图册换图阶段提交边界，补真实联系方式与表单接收方案，并转入全站发版前烟测。

## 2026-07-02 当前快照

当前源码状态以 Astro 源码和 `pnpm qa` 结果为准；旧版本号记录保留作历史。

最新阶段完成：

- 图册换图阶段已封板：办公、医疗、教育图册已完成当前轮次图片替换并通过专项验收；住宅线沿用上一阶段已完成验收结论，不在本轮重复全量复验。
- 办公椅子页已使用用户补充的本地素材重做 4 张正式 `WebP`，替换旧的偏 SKU 感图片。
- 图册专项桌面验收已完成：`output/playwright/v093-catalog-image-acceptance/report.json` 覆盖办公/医疗/教育 24 个相关页面，0 failures / 0 warnings。
- 图册移动端抽检已完成：`output/playwright/v094-catalog-mobile-spotcheck/report.json` 覆盖 6 个代表页面，390px 视口下 0 failures / 0 warnings。
- 当前阶段图册验收证据已整理到 `output/playwright/v093-catalog-image-acceptance/` 与 `output/playwright/v094-catalog-mobile-spotcheck/`。
- 酒店、办公、医疗、教育、住宅五个类目项目区已升级为每类 6 个项目/参考位；正式站只把有同名来源和图片证据的项目标为 `Caso documentado`。
- 酒店类目当前 6 个真实案例为 Grand Kempinski、Hilton DoubleTree、Pianzaihuang、Leezy、Hilton Tashkent、Radisson Lingang。
- 办公类目已用 Macwell Electronic Technology Industrial Park 替换原低质办公桌图案例，并补项目详情、来源、图片和 SEO 字段。
- 项目详情页已强制展示 `Lectura del proyecto` 说明段；有 `overview` 时使用人工项目解读，缺失时用保守模板说明，避免详情页只有数据卡片。
- 办公、医疗、教育、住宅主页面项目区之后的模块已与酒店页对齐：由大 CTA 横幅改为对应行业的 `Cooperación y respaldo` 背书模块。
- `src/styles/global.css` 已拆分为 `src/styles/partials/` 多个 CSS partial，并由 `scripts/qa-css-structure.mjs` 纳入 QA。
- 当前完整 QA：`pnpm qa`、`pnpm qa:browser`、`git diff --check` 均通过；最近构建生成 61 个 sitemap 路由，静态审计覆盖 72 个 HTML、2,934 个内部链接、755 个本地资产、450 张图片，0 failures / 0 warnings。

当前工作树不是干净状态：

- 14 个 tracked 文件已修改。
- 144 个 git status 层面的 untracked 条目；其中包含 134 张新案例 WebP、CSS partial、QA 脚本、项目证据数据、项目流程文档，以及 `tmp/` 临时素材目录。
- 处理建议见 `docs/project-brain/05-开发/09-工作树整理建议.md`。

## 当前项目阶段

项目方法已对齐，第一批项目骨架资料盘点已完成，并已进入 `v0.94 上线 QA 社媒防回退` 的持续 polish 与验收。

当前执行方式：

- 主对话：总控、源码实施、验收、项目大脑回写。
- 北极星规划与验收 Agent：只读规划和验收清单，不改源码。
- Astro 开发/QA Agent：只读 QA，不并发改源码，避免覆盖主对话修改。

## 当前本地站版本

当前 Astro 本地站点定义为 `v0.94 上线 QA 社媒防回退`。

已知状态：
- 本地预览运行中：`http://localhost:4321/`。
- `pnpm build` 已通过，静态生成 35 个页面。
- `pnpm qa:static` 已通过，覆盖 46 个 HTML、35 个 sitemap 路由、1,634 个内部链接、419 个本地资产、244 张图片、20 个 Matterport 展厅入口、37 个 iframe、71 个图册触发器和 36 个演示表单，0 failures / 0 warnings。
- `pnpm qa:browser` 已通过，覆盖核心路由桌面/移动端、首页/展厅/联系页截图、移动端菜单、`/fabrica/` 页脚附近菜单、首页移动端数据区 2x2、流程卡片图标、FAQ 分类 Tab/Accordion、横向轨道左右按钮、Showroom 弹窗、图册弹窗和联系表单演示反馈，0 failures / 0 warnings。
- 自动链接检查覆盖 35 个内部页面，无 404。
- 静态图片审计通过：无缺失本地图片，无同页重复图片 `src`。
- 首页、酒店、图册、案例、工厂、联系、办公、资源等核心路径已做浏览器检查，无断图和横向溢出。
- 联系页表单和图册下载弹窗均已验证成功反馈，并已新增显性非必填附件上传入口。
- 移动菜单已验证可展开，无横向溢出；v0.37 已压缩菜单行高和子菜单间距，390px 手机视口可看到完整主导航到 `Contacto`。
- v0.86 已修复移动端在页面底部点击汉堡菜单后导航消失的问题：移动/平板 Header 改为 fixed，菜单面板从 Header 下方展开，并把 `/fabrica/` 页脚附近打开菜单纳入 `pnpm qa:browser` 防回退检查。
- v0.87 已按用户反馈把首页信任数据区在移动端改为严格 2 行 2 列：保留 40 年经验、350,000 m² 生产车间、40,000 m² 展厅和 10,000+ 项目四个核心指标；`2,000+ personas dentro del grupo` 在移动端隐藏、桌面端保留。390px 视口验证 4 个可见指标、2 列、0 横向溢出，并已纳入 `pnpm qa:browser` 防回退检查。
- v0.88 已按用户反馈给 12 步项目流程卡片加入语义线性图标：报价、测量、设计、材料、样品、生产、质检、终检、包装、物流、安装和售后分别使用对应图标；桌面和移动端均保留原流程布局，无页面级横向溢出，并已纳入 `pnpm qa:browser` 防回退检查。
- v0.89 已按用户要求把首页 FAQ 改为 4 类 Tab + Accordion：默认显示 `Sobre productos y fabricación` 并展开第一问；移动端分类按钮横向滚动；FAQ 交互已纳入 `pnpm qa:browser` 防回退检查。
- v0.90 已按用户新增工厂设备资料重做首页 Fábrica 模块和 `/fabrica/` 页面：新增 HOMAG、Bacci、六面钻、数控板式、喷涂/水性漆、质检设备 7 张 WebP 证据图；首页使用设备证据预览，工厂页改为 6 张设备工序卡片；合作品牌 rail 改为两行横向滚动，FAQ 标题区改为居中平衡布局。
- v0.91 已按用户反馈给横向滑动轨道加入明显左右箭头按钮：合作品牌、证书图、流程步骤、首页项目卡、首页 Showroom、工厂设备证据、工厂页工序和资质卡均接入通用 `ScrollRail` 控制；按钮仅在轨道可滚动时显示，移动端浏览器 QA 已验证点击可推进且页面横向溢出为 0。
- v0.92 已修复用户反馈的两个布局回归：FAQ 移动端分类按钮不再使用负外边距横向滚动，改为 2x2 自适应按钮网格；首页 Showroom 的 `ScrollRail` 外壳已跨满内容网格，桌面端四张展厅卡重新铺满 1280px 内容区，且无内部溢出时不显示左右按钮。
- v0.93 已按用户反馈更新 Footer 社媒入口：删除 Pinterest 和 X，新增 TikTok；移动端 Footer 品牌区、社媒图标、导航列和联系区已改为居中排版，并纳入 `pnpm qa:browser` 防回退检查。
- v0.94 已把社媒集合纳入上线 QA：`pnpm qa:launch` 会检查生成 HTML 中 TikTok 是否存在、Pinterest/X 是否已移除；当前正式阻断点已从“本地演示模式”切换为 Web3Forms 后台安全约束是否补齐。
- 全站页脚已按用户反馈删除顶部深色大 CTA，当前保留品牌、社媒图标、行业入口、B2B 采购路径和联系入口。
- 站群导流入口已按用户反馈从 Footer 与 `/showroom/` 页面移除，避免把 Hymueble 流量分配到其他独立站。
- 联系页和酒店案例详情页已清理可见“待确认 / 发布前补充”类演示残留，避免老板演示时显得页面未完成。
- Footer 当前保留品牌/导航/联系信息与 Facebook、Instagram、LinkedIn、YouTube、TikTok 社媒图标入口，不再在页脚顶部重复展示大面积报价 CTA。
- 社媒入口来自公开公司资料源；TikTok 当前接入 `https://www.tiktok.com/@hongyefurniture_`，上线前仍需用户确认是否为官方账号。
- `/fabrica/` 已加入资质证据模块与认证标签。
- `/showroom/` 已加入 4 个 Matterport 展厅入口，并使用真实 Matterport 缩略图与合成展厅 Hero。
- 工厂页、联系页、酒店目录页、酒店项目页已完成一轮核心视觉去重和压缩。
- 首页与酒店页已加入第一版合作项目/认证信任条，并通过桌面和手机浏览器验收。
- 首页、项目总览、酒店案例列表、教育案例详情已接入多角度案例图；当前全站最高图片频次为 2。
- 酒店目录页、酒店分类页和展厅页已完成图片角色拆分，构建后无同页重复图片 `src`。
- 首页质量流程、工厂材料段、酒店目录 mock-up 段、联系页出口准备段已替换为真实公司过程图。
- 首页视频模块已改为右侧画幅内嵌 YouTube 播放器，支持在页面内播放和全屏，不再使用封面外链跳转。
- `/recursos/videos/` 同步使用同一个内嵌 YouTube 播放器，避免视频入口体验不一致。
- `/recursos/blog/` 与 `/recursos/videos/` 已从占位说明页升级为正式资源页：Blog 聚焦采购准备、供应商判断、质量与进口资料；Videos 聚焦企业视频、远程审核和四个 Matterport 展厅入口。
- 首页、酒店页及复用流程模块的页面已按策划案补齐 12 步项目服务流程：报价、测量、设计、材料确认、样品/样板、生产、过程质检、最终检验、出口包装、物流、安装支持、售后支持。
- 12 步项目服务流程已完成 v0.36 视觉调整：桌面端为 6 列 2 行；移动端改为 2 行横向滑动流程带，并精简西语说明以适配更紧凑的卡片。
- 案例卡和案例详情页已补“项目类型 / 空间范围 / 证据来源”字段；未知年份、房间数和金额继续留空，不编造。
- 联系页和图册下载弹窗已补显式 `Espacios a equipar` 和 `Estado del proyecto` 字段，询盘信息覆盖项目类型、空间、数量、预算和时间线；v0.55 已增加图纸、FF&E 清单、数量表、参考图和压缩包上传入口，且不放入 `Agregar detalles opcionales` 折叠区。
- 全站 35 个静态页面已接入 canonical、robots、Open Graph、Twitter Card、主题色和 Organization JSON-LD；核心页面已绑定各自分享图，不再全部使用首页默认图。
- 已新增 `/robots.txt` 与 `/sitemap.xml`，sitemap 自动读取当前路由数据并覆盖全部 35 个页面。
- 办公、医疗、教育行业入口页已接入对应 Matterport 展厅模块；住宅因暂无展厅链接保持原结构。
- 全站 Hymueble 图片资源已统一为 WebP，构建后的 `dist/assets/hymueble` 不再包含 JPG/PNG；`public/assets/hymueble` 约 11MB，`dist` 约 14MB。
- 全站 `<img>` 已补加载策略：首屏/页面主视觉 eager + high priority，下方卡片与证明图片 lazy + async decoding。
- 全站页面已补本地主视觉 preload：预加载本地相对 WebP 路径，canonical/OG 继续使用生产域名。
- 第一轮验收反馈后，首页已移除重型 `Project Solutions` 模块，改为 `Catálogo` / `Proyectos` / `Contacto` 三路采购路径分流；完整项目解决方案内容保留在 `/proyectos/`。
- 首页质量控制模块已补 6 个检查点：材料与饰面、样品/样板、生产跟进、包装检查、出货前检查、出货证据。
- 全站 Header / Footer 已接入用户提供的鸿业盛大正式透明 WebP logo，并同步到 Organization JSON-LD；390px 手机显示 logo + Hymueble，360px 极窄屏只保留 logo 以防拥挤。
- 浏览器标签页 favicon 已从旧 `H` 图标和上一版裁切图标切换为用户提供的正式 SVG logo，并补充由同一 SVG 源生成的 PNG、WebP 和 Apple Touch Icon fallback。
- `/proyectos/hoteleria/`、`/fabrica/`、`/contacto/` 已补页面内 Showroom 转化入口，客户可以在项目经验、工厂能力和提交询盘前进入 Matterport 酒店展厅或 `/showroom/` 总入口。
- Header 右侧已移除 WhatsApp 文案按钮，主 CTA 从 `Solicitar cotización` 简化为 `Cotizar`；全站新增绿色固定 WhatsApp 浮动图标，减少导航拥挤。
- 移动端 Header 已调整 `Cotizar` 与菜单按钮尺寸，避免非电脑端右侧按钮挤出屏幕。
- v0.42 全站构建已通过：34 个页面生成，首页和 `/recursos/videos/` 均使用 YouTube embed，旧 `youtu.be` 播放外链已移除。
- v0.43 已将 Hero 卖点卡片改为非按钮式图标说明区，并统一放在 lead 文案下方、CTA 上方；酒店、办公、医疗、教育、住宅及对应 `catalogo` / `proyectos` 页面均已同步。
- v0.45 已将首页 Virtual Showroom 模块从“左侧大图 + 右侧四卡”改为单栏主内容：保留四个 Matterport 展厅图卡，桌面和平板为 2x2 大画幅，手机为横向滑动卡片，删除重复的左侧酒店 featured 卡。
- v0.46 已优化 Footer `Contacto` 列：修复样式覆盖导致的 block 排版问题，改为更紧凑的图标 + 文本信息组，桌面和移动端均无横向溢出。
- v0.47 已完成老板演示路线自动走查：8 条核心页面路线均为 200，图册弹窗和联系页表单均显示成功反馈，手机菜单展开后 `Contacto` 可见，未发现横向溢出、坏图或可见半成品文案。
- v0.48 已完成视觉排版密度审计，建立全站字号、间距、文案密度、卡片节奏、CTA 层级和移动端规则。
- v0.49 已完成第一轮视觉密度代码落地：全站字号 token、section 留白、首页/showroom/footer 大标题比例、质量控制文案和采购路径文案已收紧；构建通过并完成首页、showroom、联系页和移动菜单截图验证。
- v0.50 已完成酒店路径和联系表单第二轮密度优化：酒店/目录/项目页长说明文案压缩，联系表单曾改为 3 组采购信息卡，并通过构建与抽样截图。
- v0.51 已按客户填写阻力反馈改为低阻力询盘表单：首屏只要求 5 个基础信息，邮箱、空间、数量、阶段、周期、预算等详细字段改入默认关闭的可选展开区；联系页和图册弹窗均已验证。
- v0.52 已完成全站 34 路由浏览器稳定性扫查：全部路由 200，无页面级横向溢出、坏图、非 WebP Hymueble 图片、可见半成品残留文案、核心 Header/Footer/WhatsApp 元素缺失或内部链接格式异常。
- v0.53 已替换全站正式透明公司 logo：Header、Footer、Organization JSON-LD 继续使用同一路径，favicon 改为新公司标识；首页桌面/移动端与联系页桌面浏览器验证无横向溢出、logo 加载正常。
- v0.54 已按用户反馈把 favicon 改为正式 SVG logo：页面 head 优先使用 `/favicon.svg`，PNG 与 Apple Touch Icon fallback 同步由该 SVG 源生成；联系页浏览器验证 favicon 资源 200、无横向溢出和坏图。
- v0.55 已按用户反馈在询盘表单增加附件上传入口：字段显性展示、非必填、支持多文件，接受 PDF/Office/图片/压缩包/DWG/DXF 等资料；联系页桌面/手机和图册弹窗均已验证。
- v0.56 已修复附件上传黑色按钮文字不可见问题：上传按钮显示西语 `Seleccionar archivos`，计算样式为白色文字 + 深色按钮，移动端无横向溢出。
- v0.57 已按用户反馈删除 Footer 顶部大 CTA：不再显示 `Cierre del proyecto`、`¿Listo para cotizar tu proyecto de mobiliario?`、标签组和商业响应面板，保留下方 footer 主体信息。
- v0.58 已将资源子页正式化：`/recursos/blog/` 不再是单屏占位页，`/recursos/videos/` 增加企业视频说明、视频用途卡和四个展厅图卡入口。
- v0.59-v0.62 已按 Georgebuild.com 的 B2B 建材类排版方向进行全站视觉优化：宽屏容器、编辑式 Hero、图像主导卡片、克制字号和更清晰的留白节奏。
- v0.63-v0.67 已继续降低移动端和项目页密度：流程模块容器化、合作/证书模块重做、项目总览页压缩、酒店目录卡片轻量化。
- v0.68 已把全站 Matterport 展厅入口改为站内弹窗播放，不再跳转新标签页，帮助客户留在 Hymueble 网站内浏览。
- v0.70-v0.73 已完成移动端视觉密度收敛、工厂页压缩、首页首屏 showroom 重复卡删除、旧 `.showroom-hero-card` 样式清理和全站大标题字号再收敛。
- v0.74 已补 Showroom 弹窗焦点管理：打开后焦点进入 `Cerrar`，关闭后回到原展厅入口；`asset-plan.md` 已同步当前生成素材、Matterport 和站内弹窗状态。
- v0.75 已新增 `/privacidad/` 隐私政策页，并在联系表单、图册弹窗表单和 Footer 接入隐私政策入口；sitemap 已收录该路径。
- v0.76 已强化表单正式接收前的 HTML 结构与成功反馈：共享询盘表单现在带 `method="post"`、`enctype="multipart/form-data"` 和 `data-form-purpose="project-inquiry"`，成功提示带 `role="status"` / `aria-live="polite"` 并在演示提交后获得焦点；真实接收仍需后端或表单服务。
- v0.77 已新增 `pnpm qa:static` 静态 QA gate；最新检查确认全站 21 个 Matterport 展厅入口均通过站内弹窗打开，不使用新标签页跳转，符合“把客户留在网站内”的转化目标。
- v0.78 已修复中等桌面与手机端页级横向溢出：1280px 首页 overflow 从 88px 降为 0，390px 首页 overflow 从 16px 降为 0；合作品牌、项目卡、流程等横向内容保留内部滚动，不再撑宽整页。
- v0.79 已压缩 `/proyectos/` 项目路线卡片与 `/contacto/` 辅助说明文案密度，并把联系页行内样式收束为统一 CSS；两页桌面/手机浏览器复查均为 0 溢出、0 坏图。
- v0.80 已对 14 条代表路由做桌面/手机可见长段落审计，并压缩重复 CTA、信任说明、工厂/目录/案例详情辅助文案；抽查结果为 0 溢出、0 坏图、0 超阈值可见长段落，页面与组件内旧行内文本样式已清理。
- v0.81 已确认全站 Matterport 展厅入口继续在 Hymueble 站内弹窗打开，首页点击酒店展厅后 URL 保持 `http://127.0.0.1:4321/`，弹窗 iframe 加载 `HONGYE® Furniture Hotel Showroom`；`qa:static` 已新增 iframe fullscreen 属性防回退检查。
- v0.82 已为图册下载留资补充上下文追踪：71 个图册触发器均带 `data-catalog-interest`，图册弹窗标题会显示客户请求的具体图册，表单隐藏字段 `catalog_interest` 同步记录该值；真实 PDF 发送仍等待表单接收方案。
- v0.83 已建立真实图册 PDF manifest：21 份本地图册覆盖酒店、办公、医养、教育、住宅五个行业；`pnpm qa:catalogs` 已验证 PDF 源文件存在，页面 41 个唯一图册兴趣值均能映射到 manifest。
- v0.84 后的当前实现已改为默认 Web3Forms 前端提交：WhatsApp、业务邮箱可由公开环境变量覆盖，表单不再依赖 `PUBLIC_HYMUEBLE_FORM_ACTION` 切换 demo / live；`pnpm qa:launch` 防止占位号码、演示表单或错误社媒集合进入上线构建。
- v0.85-v0.89 已新增并持续扩展真实浏览器 QA gate：`pnpm qa:browser` 覆盖核心页面视觉稳定性和关键交互，含移动端页脚菜单、移动端指标区、流程图标和 FAQ 分类折叠场景；v0.89 后完整构建、静态 QA、图册 QA 和浏览器 QA 均通过。

仍需说明：
- 当前默认 WhatsApp 已使用确认过的公开号码；部署平台仍建议显式配置 `PUBLIC_HYMUEBLE_WHATSAPP_URL`，避免未来回退到错误公开值。
- 表单前端提交流程已可用，附件上传入口已具备前端 UI；正式投放前仍需在 Web3Forms 后台完成域名限制、验证码、频率限制、告警，并做真实测试询盘。
- 证书已完成第一批视觉证据入库；合作背书已有文本信任条；本地图册 PDF 已完成第一批 manifest 入库但尚未接真实发送流程；案例年份/地区/房间数等细证据尚未完整入库。

## 本轮已经完成

- 对齐项目协作方式：主对话总控，子对话专项，结果回写 Markdown。
- 对齐项目大脑结构：更细颗粒，按阶段和专题拆分。
- 对齐资料吸收原则：先总盘点，再逐页策划，不直接继续改页面。
- 对齐页面开发规则：资料 -> 目标 -> 疑虑 -> 证据 -> 模块 -> 页面说明 -> 用户确认 -> 开发 -> 验收。
- 创建 `docs/project-brain/` 项目大脑骨架。
- 补齐 `/catalogo/`、`/proyectos/`、`/fabrica/`、`/recursos/`。
- 补齐酒店六个目录子页。
- 补齐项目详情页生成逻辑。
- 将办公、医疗、教育、住宅入口页、目录页、项目页从占位升级为可演示轻量页。
- 首页新增视频介绍、4 个案例、工厂/认证背书、showroom 入口、质量流程视觉和 12 步项目交付流程。
- 酒店页新增合作与项目背书。
- 首页和酒店页新增合作项目/认证信任条。
- 案例图接入多场景字段，Kempinski、Hilton、Raffles、Huntington 不再全站使用同一张图贯穿所有页面。
- 工厂页新增资质证据卡片。
- 联系页升级为项目采购表单，并补充空间范围、项目阶段、数量、周期、预算字段。
- 核心页面完成图片去重：构建后 34 个页面无同页重复图片 `src`。
- 全站补齐 SEO / 社交分享基础 metadata，并通过构建、静态审计和浏览器抽检。
- 新增可生成的 sitemap 与 robots，并验证本地预览环境可访问。
- 完成一轮 6 个核心页面的桌面/移动端 QA 截图，未发现断图或横向溢出；并增强办公/医疗/教育行业页的展厅转化入口。
- 完成全站图片 WebP 化：49 个 JPG/JPEG/PNG 转为 WebP，页面图片、OG 图片和构建产物均通过 WebP 审计。
- 完成图片加载策略优化：构建后 34 个页面所有 `<img>` 均有 loading/decoding 属性，并通过浏览器抽检。
- 完成主视觉 preload 优化：34 个页面均有一个本地相对 WebP preload，文件存在且不影响 OG 生产域名。
- 完成首页与项目页内容边界校正：首页只做采购路径分流，项目页承载酒店、办公、住宅地产、商业/机构四类 B2B 项目路线，避免与 `Catálogo` 和 `Proyectos` 过度重复。
- 完成首页质量控制链路增强，补齐策划案要求的材料确认、样品确认、生产跟进、包装检查、出货前检查和证据化交付口径。
- 完成公司 logo 接入：从用户提供的鸿业盛大 logo 图制作 WebP，放入全站 Header 和结构化数据。
- 完成 Showroom 转化入口补强：酒店项目、工厂、联系页不再只依赖全局导航进入展厅。
- 完成 Header 转化区视觉修正：移除导航内 WhatsApp 长按钮，改为全站固定绿色 WhatsApp 浮动入口，并缩短报价 CTA 文案。
- 完成 Header/CTA 复查与全站静态审计：确认 34 个页面 Header 状态、浮动 WhatsApp、内部链接和 WebP 图片约束均通过。
- 完成 12 步流程视觉改版：桌面 6 列 2 行，移动端 2 行横向滑动，并精简每步文案以保持版面稳定。
- 完成移动端菜单体验修正：菜单层可独立滚动，行高和子菜单间距收紧，390px 视口展开后完整导航可见。
- 完成全站页脚升级：参考用户给的模板方向，改为项目询盘 CTA + 品牌/行业/采购/能力信息区，并通过桌面与移动端浏览器检查。
- 完成站群导流移除：Footer 不再显示 `Sitios del grupo`，`/showroom/` 不再显示站群参考区，生成后的 34 个 HTML 页面无站群外链残留。
- 完成演示残留清理：联系页生产基地说明改为正式商业表述，酒店案例详情页的“待确认”交付字段改为“内部项目资料已记录”的专业口径。
- 完成 Footer 高级视觉重设：强化报价转化、Showroom 入口和品牌信任；当前社媒入口为 Facebook、Instagram、LinkedIn、YouTube、TikTok 五个平台。
- 完成首页 Showroom 去重改版：删除左侧重复酒店大卡，把四个展厅卡片升级为主要视觉内容，并完成桌面、平板、手机截图验收。

## 当前正在处理

第四轮策划案对齐、案例证据增强、SEO/性能基础补齐、首页/项目页信息边界、质量控制、品牌主体、工厂设备证据链和 Showroom 转化路径 polish。

重点输入：
- `鸿业家具西班牙语独立站策划案.xlsx`
- `hymueble-导航栏.png`
- `hymueble-页面.png`
- `色卡.pdf`
- 当前 Astro v0.94
- 现有站群链接（仅作为资料来源，不作为站内导流入口）

## 下一步建议

1. 深读公司资料包、资质文件、图册和案例资料，建立证据库。
2. 将真实案例字段补入项目详情页：国家城市、年份、酒店类型、房间数量、供应范围、材质/工艺。
3. 继续补真实证书、合作 logos、工厂设备图片和更高质量 Matterport 展厅截图。
4. 做下一轮视觉 polish：移动端细节、案例详情页、资源页和行业轻量页。
5. 继续让子对话输出报告，主对话筛选后回写项目大脑。

## 阻塞问题

- 真实 WhatsApp 号码未提供。
- 品牌名已暂定统一使用 `Hymueble`；若后续老板要求再统一替换。
- 表单后端接收方式未定。
- 域名、部署平台、统计工具未定。
- 案例公开口径需要在案例证据库中逐项记录。

## 最近重要决策

- 项目不再依赖聊天上下文作为长期记忆。
- 当前站点升级为 v0.76 表单正式提交结构与成功反馈可访问性预览版。
- 一期所有导航和卡片点击必须避免 404，即使部分页面先做轻量版。
- 子对话不并发写源码；主对话负责合并和验收。
- 首页不承载完整项目解决方案长模块；首页只负责采购路径分流，完整项目解决方案留在 `/proyectos/`，避免和 `Catálogo`、`Proyectos` 互相重复。

## 需要优先读取的文件

1. `docs/project-brain/00-总控/01-当前状态.md`
2. `docs/project-brain/00-总控/02-决策记录.md`
3. `docs/project-brain/00-总控/03-下一步任务.md`
4. `docs/project-brain/01-资料/00-资料总地图.md`
5. `docs/project-brain/03-页面/00-页面总表.md`
6. `docs/project-brain/06-验收/06-策划案差距审计-v0.8.md`
